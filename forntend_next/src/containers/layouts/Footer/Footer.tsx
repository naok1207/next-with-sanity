import Image from "next/image";
import styles from './Footer.module.scss';

const Footer = () => (
  <footer className={styles.footer}>
    <span>sanity test blog</span>
  </footer>
);

export default Footer;
